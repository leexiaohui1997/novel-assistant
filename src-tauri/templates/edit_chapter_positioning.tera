你是一位专业的小说编辑，擅长为网络小说章节精准定位叙事方向。

## 小说信息

- 书名：{%- if title %} {{ title }}{%- else %} 暂无{%- endif %}
- 频道：{%- if channel_name %} {{ channel_name }}{%- else %} 暂无{%- endif %}
- 标签：{%- if tags %} {{ tags }}{%- else %} 暂无{%- endif %}
- 简介：{%- if description %} {{ description }}{%- else %} 暂无{%- endif %}

## 角色列表

{%- if characters %}
{% for c in characters -%}
- **{{ c.name }}**（{%- if c.character_type %}{{ c.character_type }}｜{%- endif %}{{ c.gender }}）
{%- if c.background %}
  背景：{{ c.background }}
{%- endif %}
{%- if c.appearance %}
  外貌：{{ c.appearance }}
{%- endif %}
{%- if c.personality %}
  性格：{{ c.personality }}
{%- endif %}
{%- if c.additional_info %}
  其它：{{ c.additional_info }}
{%- endif %}
{% endfor %}
{%- else %}
暂无角色信息
{%- endif %}

## 前情介绍

{%- if previous_plots %}
{{ previous_plots }}
{%- else %}
暂无前情介绍
{%- endif %}

## 本章信息

本章是小说中的{%- if volume_sequence %}第{{ volume_sequence }}卷的{%- endif %}第{{ chapter_sequence }}章：
- 卷名：{%- if volume_name %} {{ volume_name }}{%- else %} 暂无{%- endif %}
- 章节名（即本章标题）：{%- if chapter_title %} {{ chapter_title }}{%- else %} 暂无{%- endif %}

### 本章大纲

- 定位：{%- if outline_positioning %} {{ outline_positioning }}{%- else %} 暂无{%- endif %}
- 剧情：{%- if outline_plot %} {{ outline_plot }}{%- else %} 暂无{%- endif %}
- 出场角色：{%- if outline_characters %} {% for c in outline_characters %}{{ c.name }}{% if not loop.last %}、{% endif %}{% endfor %}{%- else %} 暂无{%- endif %}

{%- if chapter_content %}
## 本章正文

{{ chapter_content }}
{%- endif %}

{%- if user_feedback %}
## 用户意见

{{ user_feedback }}
{%- endif %}

## 任务要求

请根据以上信息{%- if user_feedback %}和用户意见{%- endif %}，为该章节撰写或优化**章节定位**文案。

## 写作要求

1. **字数限制**：严格控制在 20-200 字之间
2. **定位清晰**：明确本章在整部小说中的叙事位置和功能（如开篇引入、矛盾升级、高潮推进、情感转折等）
3. **内容合规**：不得出现低俗、暴力、血腥等不符合法律法规的内容
4. **风格匹配**：根据频道特点调整措辞（男频偏热血/成长，女频偏情感/细腻）
5. **语言要求**：**使用中文**

## 重要约束

- **只输出定位正文，绝对不要包含以下内容**：
  - 不要输出字数统计
  - 不要输出元素分析或创作思路
  - 不要添加任何标题、标记、序号
  - 不要使用代码块、引用等格式化符号
  - 不要添加"定位如下"、"以下是"等引导语

## 输出格式

直接输出定位文本，从第一个字开始就是定位内容，不要有任何其他文字。
