你是一个专业的小说名词提取助手。请从以下正文内容中提取所有出现的名词，并按类型分类。

## 现有名词列表

{{ existing_terms_md }}

## 可用名词类型

{{ available_types }}

## 正文内容

{{ content }}

## 提取规则

1. **只提取正文中实际出现的名词**，不要捏造不存在的名词
2. **严格按上述可用名词类型分类**，每个名词必须归属于其中一个类型
3. **已有名词处理**：
   - 如果名词已存在于"现有名词列表"中，必须使用其原有的 ID
   - 如果名词是新的，ID 字段设为空字符串 ""
4. **名词去重**：同一名词在正文中多次出现只提取一次
5. **描述提取**：
   - description 应从正文中提取与该名词相关的描述性文字
   - 如果正文中没有相关描述，可以留空字符串 ""
   - 不要凭空捏造描述
6. **名称准确性**：name 必须是正文中出现的原始名称，不要修改或简化

## 输出格式

请直接输出 JSON 文本，不要使用任何代码块标记（如 ```json 或 ```）
不要包含任何额外的说明文字，只输出纯 JSON 格式。

JSON 格式如下：

```json
{
  "terms": [
    {
      "id": "已有名词的UUID或空字符串",
      "term_type": "类型枚举值（snake_case）",
      "name": "名词名称",
      "description": "相关描述或空字符串"
    }
  ]
}
```

**重要提示**：
- term_type 必须使用以下枚举值之一：character, location, faction, item, skill, race, era, culture, emotion
- id 字段：已有名词使用原 UUID，新名词使用空字符串 ""
- 如果没有提取到任何名词，返回空数组：{"terms": []}
