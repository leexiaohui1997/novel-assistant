本技能定义了「按需优化或补全小说基础信息（书名 / 标签 / 简介）」的处理流程：从用户对话中提取已有信息 → 判断本次需要返回的字段子集 → 按固定 JSON 结构输出结果。

## 输入说明

- 当前小说的书名、频道、标签、简介等信息将由用户在对话消息中提供。
- 你需要**自行从用户消息中提取**这些已有信息，作为生成或优化的依据。
- 当某项信息用户未提供时，按该字段缺省的前提下进行创作，不要凭空臆造已有内容。
- 若用户在消息中给出了"优化意见 / 修改要求"，必须结合该意见进行生成。

## 任务判断

- 根据用户对话语义，自行判断本次需要返回的字段子集，候选字段仅限：`title`、`tags`、`introduction`。
- **仅返回用户实际请求的字段**：未被请求的字段必须从输出 JSON 中省略，禁止凭空生成。
- 各字段创作约束：
  1. `title`：简洁有力、不超过 15 个字，符合频道调性，不要包含书名号或多余引号。
  2. `introduction`：50-500 字之间，开头抓人、设置悬念、风格匹配频道、合规无低俗暴力，仅输出简介正文。
  3. `tags`：只能从用户消息中给出的"可用标签列表"中选择；若用户未提供可用标签列表，则不要返回 `tags` 字段。

## 输出格式

- **仅输出一个 JSON 对象**，不要输出任何解释性文字。
- **禁止使用 ```json 代码块标记或任何 Markdown 格式**，直接输出纯 JSON 文本。
- 字段命名固定为 `title`、`introduction`、`tags`。
- `tags` 字段结构固定为：`{ "main_category": [...], "theme": [...], "character": [...], "plot": [...] }`，每一项均为字符串数组，未推荐的子类使用空数组。

完整结构示例（实际输出时未被请求的字段需省略）：

{
  "title": "...",
  "introduction": "...",
  "tags": {
    "main_category": ["..."],
    "theme": ["..."],
    "character": ["..."],
    "plot": ["..."]
  }
}
